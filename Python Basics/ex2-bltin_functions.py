quote = " to be or not to be"
print(
    f"new string created: {quote.replace('be', 'me')}"
)  # here a new string is created

print(f"original string: {quote}")  # to be or not to be, because strings are immutable
